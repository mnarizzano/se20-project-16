#!/usr/bin/env bash

# NOTE: must have pip3 and git installed

# Uncomment following line if having error with wrapt while installing Tensorflow
# pip3 install wrapt --upgrade --ignore-installed

# Uncomment following line if PyQt5 could not load the Qt platform plugin "xcb" in "" even though it was found.
# sudo apt-get install libxcb-xinerama0

pip3 install -r src/requirements.txt
